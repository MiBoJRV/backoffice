import styled from "styled-components";

export const CustomerContent = styled.div`
    .customers-title {
      color: #1C0371;
      font-size: 36px;
      font-style: normal;
      font-weight: 700;
      line-height: normal;
    }
`;